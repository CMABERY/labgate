# {{NAME}}.lab

Working area for the `{{NAME}}` repo, a sibling of this directory. Nothing here
ships. Create whatever helps.

- `handoff/<branch>.md`: the builder's declaration for the promoter
  (`handoff/TEMPLATE.md`).
- `notes/YYYY-MM-DD-topic.md`: decisions, findings, rejected alternatives.
  Ten lines is normal.
- `plans/`: feature plans. `prompts/`: prompts used to drive agents.
- `experiments/`: throwaway scripts. Never move these into the repo; rewrite
  them there.
- `runs/`: outputs, sample data, benchmark numbers.
- Code that should ship is written in a repo worktree, not here.
- No secrets. No exported session transcripts.
