# handoff: <branch>

Behavior change: <one sentence: what main will do after this merge that it does not do now>

Scaffolding left on the branch: <files, debug output, commented code — everything the promoter should delete>

Uncertain: <what was not verified, and why>

README.md: <updated | not updated — what needs to change>

Verified: <exact commands run and their results>
